// const express = require("express");
// const router = express.Router();
// const postController = require("../controllers/post-controller");

// router.route("/").post(postController.postCreate);

// //add additional methods to this route(.put and .delete) (HW12P2)
// //method override in app.js
// router.route("/:_id").get(postController.postGet).put(postController.postUpdate).delete(postController.postDelete);


// module.exports = router;